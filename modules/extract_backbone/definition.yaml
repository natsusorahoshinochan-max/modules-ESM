module_id: convert.extract_backbone
version: 1.0.0
display_name: Extract Backbone
category: conversion
description: "Produces a new ProteinStructure with only N, CA, C, O backbone atoms."
input_ports:
  - name: structure
    type_id: protein.structure
    display_name: Structure
    description: "Protein structure to extract backbone from."
output_ports:
  - name: structure
    type_id: protein.structure
    display_name: Backbone Structure
    description: "ProteinStructure with only backbone atoms."
module_api: "1.0"
