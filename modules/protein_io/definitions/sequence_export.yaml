schema_version: "2.0.0"
node_type_id: protein_io.export_sequence
version: "2.0.0"
title: Export protein sequence
summary: Serializes one protein sequence as a validated Run-bound artifact.
category: protein_io
inputs:
  - name: sequence
    port_type_id: protein.sequence
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Protein sequence to serialize as canonical FASTA.
outputs:
  - name: standalone_artifact
    port_type_id: protein_io.artifact_payload
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Cache-safe FASTA bytes for opaque Run-bound publication.
    artifact_kind: standalone
parameter_groups: []
node_parameters: {}
