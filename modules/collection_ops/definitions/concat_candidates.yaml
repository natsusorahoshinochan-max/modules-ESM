node_type_id: collection_ops.concat_candidates
title: Concatenate Candidate collections
summary: Preserves exact Candidate identities while concatenating connected input partitions in declared Port order.
category: collection_ops
inputs:
  - name: candidates_a
    port_type_id: candidate.collection
    required: false
    multiplicity: one
    scientific_meaning: Optional first ordered Candidate collection partition.
  - name: candidates_b
    port_type_id: candidate.collection
    required: false
    multiplicity: one
    scientific_meaning: Optional second ordered Candidate collection partition.
  - name: candidates_c
    port_type_id: candidate.collection
    required: false
    multiplicity: one
    scientific_meaning: Optional third ordered Candidate collection partition.
outputs:
  - name: candidates
    port_type_id: candidate.collection
    required: true
    multiplicity: one
    scientific_meaning: Ordered union retaining every exact input Candidate identity, lineage, and producer slot.
parameter_groups: []
node_parameters: {}
