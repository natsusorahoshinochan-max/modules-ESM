module_id: import.structure
version: 1.0.0
display_name: Import Structure
category: input
description: "Reads a PDB file and produces a ProteinStructure."
input_ports: []
output_ports:
  - name: structure
    type_id: protein.structure
    display_name: Structure
    description: "The imported protein structure."
parameters:
  - name: file_path
    type: str
    default: ""
    display_name: File Path
    description: "Path to the PDB file to import."
module_api: "1.0"
