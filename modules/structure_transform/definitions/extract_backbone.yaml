schema_version: "2.1.0"
node_type_id: structure_transform.extract_backbone
version: "4.0.0"
title: Extract protein backbone
summary: Projects one resolved structure residue axis to the exact canonical N-CA-C-O backbone contract.
category: structure_transform
inputs:
  - name: residue_axis
    port_type_id: structure_transform.resolved_residue_axis
    port_type_version: "4.0.0"
    required: true
    multiplicity: one
    scientific_meaning: The sole admitted residue identities, parent names, segment topology, coordinates, and complete-backbone mask to project.
outputs:
  - name: backbone
    port_type_id: structure_transform.backbone_structure
    port_type_version: "4.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Canonical parent-residue ATOM-only N-CA-C-O structure with axis-defined chain breaks.
parameter_groups: []
node_parameters: {}
