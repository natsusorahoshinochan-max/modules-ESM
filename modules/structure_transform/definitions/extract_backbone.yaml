schema_version: "2.0.0"
node_type_id: structure_transform.extract_backbone
version: "2.0.0"
title: Extract protein backbone
summary: Converts one single-model full-atom structure to the exact canonical N-CA-C-O backbone contract.
category: structure_transform
inputs:
  - name: structure
    port_type_id: protein.structure
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Full-atom single-model structure to convert explicitly.
outputs:
  - name: backbone
    port_type_id: structure_transform.backbone_structure
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Canonical ATOM-only N-CA-C-O structure with explicit chain breaks.
parameter_groups: []
node_parameters: {}
