schema_version: "2.1.0"
node_type_id: structure_transform.extract_sequence
version: "3.0.0"
title: Extract structure sequence
summary: Derives a residue-correspondent protein sequence from one single-model structure.
category: structure_transform
inputs:
  - name: structure
    port_type_id: protein.structure
    port_type_version: "3.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Single-model structure whose ordered ATOM residues define the sequence.
outputs:
  - name: sequence
    port_type_id: protein.sequence
    port_type_version: "2.1.0"
    required: true
    multiplicity: one
    scientific_meaning: Concatenated chain-order sequence with chain-qualified residue correspondence.
parameter_groups: []
node_parameters: {}
