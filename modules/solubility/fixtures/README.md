# SoluProt golden fixtures

The input and full-mode value are the first official `data/test.fa` /
`data/test.csv` case embedded in the locked SoluProt 1.1.0 wheel under the
agreed external dependency workspace. The no-TM value is produced from that
same official input by the wheel's distinct no-TMHMM model and is retained
separately. These files are excluded from production package data.

The Protein-Sol input and expected rows retain two exact October 2017 upstream
results from the locked external dependency for independent ordered batch
inference.
