schema_version: "2.0.0"
node_type_id: structure_annotation.secondary_structure_agreement
version: "2.0.0"
title: Observe secondary-structure agreement
summary: Observes exact SS8 agreement for one identified subject against one identified reference on one identical residue layout.
category: structure_annotation
inputs:
  - name: subjects
    port_type_id: candidate.collection
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Exactly one Candidate whose annotation agreement is being observed.
  - name: references
    port_type_id: candidate.collection
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Exactly one reference Candidate that supplied the expected annotation.
  - name: expected
    port_type_id: structure_annotation.secondary_structure_track
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Expected exact SS8 values carrying the subject residue layout.
  - name: observed
    port_type_id: structure_annotation.secondary_structure_track
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Observed exact SS8 values carrying the same subject residue layout.
outputs:
  - name: scores
    port_type_id: score.collection
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: One typed pairwise secondary-structure agreement Observation for the exact subject and reference Candidates.
parameter_groups: []
node_parameters: {}
