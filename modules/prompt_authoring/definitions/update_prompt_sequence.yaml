schema_version: "2.0.0"
node_type_id: prompt_authoring.update_prompt_sequence
version: "2.0.0"
title: Update ProteinPrompt sequence
summary: Replaces only the sequence track while preserving the effective layout and every unaffected track.
category: prompt_authoring
inputs:
  - name: protein_prompt
    port_type_id: protein.prompt
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Validated multi-track ProteinPrompt whose sequence track will be replaced.
  - name: sequence
    port_type_id: protein.sequence
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Complete legal amino-acid sequence ordered by the Prompt's effective residue layout.
outputs:
  - name: protein_prompt
    port_type_id: protein.prompt
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: ProteinPrompt with only its sequence assignments changed.
parameter_groups: []
node_parameters: {}
