metric_id: structure.plddt.per_residue
title: Per-residue pLDDT
description: Per-residue predicted local distance difference confidence on the canonical zero-to-100 scale, with the exact prediction-input or resolved-structure residue axis carried by every Observation.
value_shape: per_residue
unit: dimensionless
direction: higher_is_better
canonical_range:
  minimum: 0
  maximum: 100
granularity: residue
aggregation_semantics:
  kind: none
observation_context_schema:
  type: object
  properties:
    kind:
      const: intrinsic
  required: [kind]
  additionalProperties: false
validation_contract:
  finite: true
  masking:
    allow_null: true
