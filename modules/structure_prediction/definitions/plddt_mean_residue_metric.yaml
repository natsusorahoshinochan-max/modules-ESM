metric_id: structure.plddt.mean_residue
title: Mean-residue pLDDT
description: Equal-weight arithmetic mean of canonical per-residue pLDDT over the exact prediction-input or resolved-structure residue population carried by every Observation.
value_shape: scalar
unit: dimensionless
direction: higher_is_better
canonical_range:
  minimum: 0
  maximum: 100
granularity: candidate
aggregation_semantics:
  kind: equal_weight_mean
  source_metric: structure.plddt.per_residue
  included_values: non_null_per_residue_values_on_exact_axis
observation_context_schema:
  type: object
  properties:
    kind:
      const: intrinsic
  required: [kind]
  additionalProperties: false
validation_contract:
  finite: true
