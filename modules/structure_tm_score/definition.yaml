module_id: structure.tm_score
version: 1.0.1
display_name: TM-score
category: scoring
description: "Computes standard per-residue TM-score from a pre-computed StructureAlignment, normalized by the reference length."
input_ports:
  - name: alignment
    type_id: structure.alignment
    display_name: Alignment
    description: "Pre-computed StructureAlignment."
output_ports:
  - name: scores
    type_id: score.collection
    display_name: Scores
    description: "Reference-normalized TM-score entry with normalization length and reference coverage."
parameters:
  - name: candidate_id
    type: string
    default: ""
    display_name: Candidate ID
    description: "Candidate evaluated by this alignment. Required for candidate-bound scoring."
  - name: score_id
    type: string
    default: "tm_score"
    display_name: Score ID
    description: "Identifier for the output score entry."
module_api: "1.0"
