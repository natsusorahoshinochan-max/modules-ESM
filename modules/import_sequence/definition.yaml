module_id: import.sequence
version: 1.0.0
display_name: Import Sequence
category: input
description: "Reads a FASTA file and produces a ProteinSequence."
input_ports: []
output_ports:
  - name: sequence
    type_id: protein.sequence
    display_name: Sequence
    description: "The imported protein sequence."
parameters:
  - name: file_path
    type: str
    default: ""
    display_name: File Path
    description: "Path to the FASTA file to import."
module_api: "1.0"
