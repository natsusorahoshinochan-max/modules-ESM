schema_version: "2.1.0"
metric_id: structure.plddt.mean_residue
version: "2.1.0"
title: Mean-residue pLDDT
description: Equal-weight arithmetic mean of the canonical per-residue pLDDT values over every valid protein residue.
value_shape: scalar
unit: dimensionless
direction: higher_is_better
canonical_range:
  minimum: 0
  maximum: 100
granularity: candidate
aggregation_semantics:
  kind: equal_weight_mean
  source_metric: structure.plddt.per_residue@2.1.0
  included_values: valid_protein_residues
observation_context_schema:
  type: object
  properties:
    kind:
      const: intrinsic
  required: [kind]
  additionalProperties: false
validation_contract:
  finite: true
