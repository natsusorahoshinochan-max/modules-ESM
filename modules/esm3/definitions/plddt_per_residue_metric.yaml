schema_version: "2.1.0"
metric_id: structure.plddt.per_residue
version: "2.1.0"
title: Per-residue pLDDT
description: Per-residue predicted local distance difference confidence on the canonical zero-to-100 scale and exact subject residue axis.
value_shape: per_residue
unit: dimensionless
direction: higher_is_better
canonical_range:
  minimum: 0
  maximum: 100
granularity: residue
aggregation_semantics:
  kind: none
observation_context_schema:
  type: object
  properties:
    kind:
      const: intrinsic
  required: [kind]
  additionalProperties: false
validation_contract:
  finite: true
  masking:
    allow_null: true
