schema_version: "2.0.0"
metric_id: structure.ptm
version: "2.0.0"
title: Predicted template modelling score
description: Provider-native ESM-3 pTM normalized only from its documented scalar representation.
value_shape: scalar
unit: dimensionless
direction: higher_is_better
canonical_range:
  minimum: 0
  maximum: 1
granularity: candidate
aggregation_semantics:
  kind: none
observation_context_schema:
  type: object
  properties:
    kind:
      const: intrinsic
  required: [kind]
  additionalProperties: false
validation_contract:
  finite: true
