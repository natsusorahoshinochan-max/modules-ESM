schema_version: "2.0.0"
metric_id: structure.pae
version: "2.0.0"
title: Predicted aligned error
description: Exact ESM-3 residue-pair PAE matrix in angstroms after only the documented local special-token normalization.
value_shape: residue_pair_matrix
unit: angstrom
direction: lower_is_better
canonical_range:
  minimum: 0
  maximum: 31.5
granularity: residue_pair
aggregation_semantics:
  kind: none
observation_context_schema:
  type: object
  properties:
    kind:
      const: intrinsic
  required: [kind]
  additionalProperties: false
validation_contract:
  finite: true

