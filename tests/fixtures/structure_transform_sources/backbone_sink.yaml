schema_version: "2.0.0"
node_type_id: contract_test.backbone_sink
version: "2.0.0"
title: Backbone sink
summary: Accepts only the exact nominal backbone structure contract.
category: contract_test
inputs:
  - name: backbone
    port_type_id: structure_transform.backbone_structure
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Canonically validated backbone-only structure.
outputs:
  - name: accepted
    port_type_id: text
    port_type_version: "2.0.0"
    required: true
    multiplicity: one
    scientific_meaning: Literal acceptance marker.
parameter_groups: []
node_parameters: {}
