node_type_id: contract_test.multi_objective_selection_scores
title: Canonical multi-objective selection scorer
summary: Emits literal fixed-reference and paired-reference unit-interval fixture Observations for already admitted Candidates.
category: contract_test
inputs:
  - name: candidates
    port_type_id: candidate.collection
    required: true
    multiplicity: one
    scientific_meaning: Exact admitted folded Candidate subjects.
  - name: references
    port_type_id: candidate.collection
    required: true
    multiplicity: one
    scientific_meaning: Exact admitted fixed and per-subject reference Candidates.
  - name: pairing
    port_type_id: candidate.pairing
    required: true
    multiplicity: one
    scientific_meaning: Exact admitted subject-to-counterpart Candidate mapping.
outputs:
  - name: scores
    port_type_id: score.collection
    required: true
    multiplicity: one
    scientific_meaning: Isolated fixed-reference and per-subject counterpart synthetic selection Observations joined by complete Candidate Data Reference.
parameter_groups: []
node_parameters: {}
