node_type_id: contract_test.prediction_confidence_fact_source
title: Exact prediction-confidence fact conformance source
summary: Associates one exact prediction axis and admitted structure digest with Method-specific subjectless confidence.
category: contract_test
inputs:
  - name: prediction_axis
    port_type_id: structure_prediction.prediction_residue_axis
    required: true
    multiplicity: one
    scientific_meaning: One exact sequence-parent prediction axis.
  - name: structures
    port_type_id: candidate.collection
    required: true
    multiplicity: one
    scientific_meaning: One exact prediction structure Candidate.
outputs:
  - name: confidence_facts
    port_type_id: structure_prediction.confidence_facts
    required: true
    multiplicity: one
    scientific_meaning: One exact Method-specific subjectless prediction-confidence fact.
parameter_groups:
  - id: missingness
    title: Fixture missingness
    parameters:
      - missing_loop_plddt
node_parameters:
  missing_loop_plddt:
    parameter_scope: scientific
    scientific_meaning: Publish a lawful null pLDDT value only for A:loop.
    required: false
    default: false
    value_contract:
      type: boolean
