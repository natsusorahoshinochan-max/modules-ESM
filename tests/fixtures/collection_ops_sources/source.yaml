node_type_id: contract_test.collection_ops_source
title: Collection operations source
summary: Emits one independently partitioned Candidate collection fixture for downstream admission.
category: contract_test
inputs: []
outputs:
  - name: candidates
    port_type_id: candidate.collection
    required: true
    multiplicity: one
    scientific_meaning: Exact generated fixture Candidates.
parameter_groups:
  - id: fixture
    title: Fixture
    parameters:
      - candidate_count
node_parameters:
  candidate_count:
    parameter_scope: scientific
    scientific_meaning: Exact number of fixture Candidates to emit.
    value_contract:
      type: integer
      minimum: 0
      maximum: 3
    required: true
