node_type_id: contract_test.collection_ops_scorer
title: Collection operations scorer
summary: Emits controlled intrinsic fixture Observations for supplied Candidates.
category: contract_test
inputs:
  - name: candidates
    port_type_id: candidate.collection
    required: true
    multiplicity: one
    scientific_meaning: Exact fixture Candidates to observe.
outputs:
  - name: scores
    port_type_id: score.collection
    required: true
    multiplicity: one
    scientific_meaning: One controlled intrinsic Observation per input Candidate.
parameter_groups: []
node_parameters: {}
